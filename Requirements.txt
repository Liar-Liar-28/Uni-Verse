googletrans==4.0.0rc1
SpeechRecognition
gTTS
playsound
PyAudio
⚡ Notes:

PyAudio is required only if you plan to record audio directly (not just use .wav files).

You can save this as requirements.txt and install all dependencies with:

pip install -r requirements.txt
